
<style>
            body {
                font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                color: #333;
                line-height: 1;
            }
            a:link, a:visited {
                color: inherit;
            }
            .ann-sentence {
                width: 100%;
                display: flex;
                flex-direction: row;
                flex-wrap: wrap;
            }
            .ann-word {
                display: flex;
                flex-wrap: nowrap;
            }
            .ann-morph {
                display: block;
                padding: 0 0 5px;
                background-image: repeating-linear-gradient(-45deg, rgba(0,0,0,0) 0, rgba(0,0,0,0) 3px, rgba(0,0,0,0.175) 4px, rgba(0,0,0,0) 5px, rgba(0,0,0,0) 8px);
                border: solid 1px rgba(0,0,0,0.075);
                text-align: center;
            }
            .ann-morph:first-child .ann-text {
                text-align: right;
            }
            .ann-morph:last-child .ann-text {
                text-align: left;
            }
            .ann-morph:only-child .ann-text {
                text-align: center;
            }
            .ann-morph + .ann-morph {
                margin-left: 3px;
            }
            .ann-text, .ann-punct {
                display: block;
                font-size: 30px;
                line-height: 1.25;
            }
            .ann-gloss, .ann-punct::after {
                display: block;
                font-size: 14px;
                line-height: 1.5;
                padding: 0 1px;
            }
            .ann-punct::after {
                content: " ";
            }
            .ann-word + .ann-word,
            .ann-punct + .ann-word {
                margin-left: 24px
            }

            .ann-sandhi {
                display: block;
                margin-left: 3px;
                margin-right: 3px;
                width: 6px;
                background-color: #ccc;
                background-image: repeating-linear-gradient(45deg, rgba(0,0,0,0) 0, rgba(0,0,0,0) 3px, rgba(0,0,0,0.175) 4px, rgba(0,0,0,0) 5px, rgba(0,0,0,0) 8px);
                border: solid 1px rgba(0,0,0,0.075);
            }

            .ann-empty {
                opacity: 0.3333;
            }

            .pg-all {
                display: flex;
            }
            .pg-main {
                width: 50%;
                flex-grow: 1;
            }
            .pg-side {
                width: 20%;
                flex-grow: 1;
            }

            .src-inherited { background-color: #fff; }

            .src-hungarian { background-color: #ffc; }
            .src-greek { background-color: #fee; }
            .src-slavic { background-color: #eef; }
        </style>


{{#each blocks}}

    {{#with glossed}}
        {{#each this}}
            <p class="ann-sentence">
                {{#each this}}
                    {{#with word}}
                        <span class="ann-word">
                            {{> word }}
                        </span>
                    {{/with}}

                    {{#with sandhi}}
                        <span class="ann-sandhi"></span>
                    {{/with}}

                    {{#with punct}}
                        <span class="ann-punct">{{this}}</span>
                    {{/with}}
                {{/each}}
            </p>
        {{/each}}
    {{else}}
        {{{text}}}
    {{/with}}

{{/each}}